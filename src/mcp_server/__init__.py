"""Headless MCP server package for the embedded agent tool surface.

Named ``mcp_server`` (not ``mcp``) so it can never shadow the installed
``mcp`` SDK package.  See :mod:`src.mcp_server.stdio_server` for the
stdio server entry point (``python -m src.mcp_server.stdio_server``).
"""
